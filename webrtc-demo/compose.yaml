services:
  echo:
    build: .
    restart: unless-stopped
    environment:
      PUBLIC_IP: ${PUBLIC_IP:-}
    ports:
      - "${HTTP_BIND:-127.0.0.1}:${HTTP_PORT:-8080}:8080/tcp"
      - "20000-20100:20000-20100/udp"
